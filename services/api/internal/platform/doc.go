// Package platform anchors the api service's platform-level packages
// (config, health, http, mongo, outbox, inbox, idempotency). It carries no
// code itself; see the subpackages and their READMEs for conventions.
package platform
