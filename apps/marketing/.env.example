# Public canonical origin used by metadata, robots and sitemap.
NEXT_PUBLIC_MARKETING_URL=https://www.example.com

# Server-side API origin used by the waitlist proxy. Do not expose credentials.
OBIARA_API_BASE_URL=https://api.example.com
