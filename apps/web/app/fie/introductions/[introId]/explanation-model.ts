/**
 * The introduction UI fails closed until the durable introduction lifecycle is
 * composed. Matching consent is loaded separately from the consent API.
 */
export const introductionExplanationAvailability = "unavailable" as const;
